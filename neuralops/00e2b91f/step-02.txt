-- Pre-deploy checklist query
SELECT 
    id, 
    type, 
    status, 
    error, 
    attempts, 
    max_retries 
FROM 
    jobs 
WHERE 
    status IN ('queued', 'waiting_approval') 
    AND updated_at > NOW() - INTERVAL '1 day' 
ORDER BY 
    created_at DESC;